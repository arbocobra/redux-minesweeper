// import { createSlice } from '@reduxjs/toolkit';

// export const activeCellSlice = createSlice({
//     name: 'activeCell',
//     initialState,
//     reducers: {
//         setCell: (state, action) => state.id = action.payload,
//         setContent: (state, action) => state.content = getContent(action.payload),
//         clearContent: (state) => state = ''
//     }
// })

// export const { setCell, setContent, clearContent } = activeCellSlice.actions;
// export const selectActiveCell = (state) => state.activeCell;
// export default activeCellSlice.reducer;